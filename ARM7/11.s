AREA PGM,CODE,READONLY
NUMBER 		RN 1
NUM 		RN 2
DIVIDEND 	RN 3
DIVISOR 	RN 4
QUOTIENT 	RN 5
REMAINDER	RN 6
ENTRY
	LDR NUMBER,=12321
	MOV NUM,#0
	MOV DIVISOR,#10
	MOV DIVIDEND,NUMBER
	MOV R7,#10
LOOP
	BL DIV
	MUL NUM,R7,NUM
	ADD NUM,REMAINDER,NUM
	CMP QUOTIENT,#0
	MOVNE DIVIDEND,QUOTIENT
	BNE LOOP
	CMP NUMBER,NUM
	MOVEQ R8,#1
	MOVNE R8,#0
STOP B STOP
DIV
	MOV QUOTIENT,#0
REM
	SUBS DIVIDEND,DIVIDEND,DIVISOR
	ADDPL QUOTIENT,QUOTIENT,#1
	BPL REM
	ADDMI REMAINDER,DIVISOR,DIVIDEND
	MOV PC,LR
	END